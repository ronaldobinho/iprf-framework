// JMH microbenchmarks for rule evaluation plus a load harness against the
// evaluate endpoint, exposed as `./gradlew benchmark`.
//
// Results are published with the environment fingerprint that produced them —
// documented as reproducible output, never as a performance claim.
//
// Populated in Session 6.1.
